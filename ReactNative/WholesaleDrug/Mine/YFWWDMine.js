import React, {Component} from 'react';
import {
    DeviceEventEmitter,
    Image,
    NativeEventEmitter,
    Platform,
    StyleSheet,
    Text,
    TextInput,
    TouchableOpacity,
    View,
} from 'react-native';

export default class YFWWDMine extends Component {

    static navigationOptions = ({navigation}) => ({
        tabBarVisible: true,
        header: null,
    });

    render() {
        return (<View></View>)
    }
}